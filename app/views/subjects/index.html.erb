<div class="wrapper wrapper-content">
  <div class="row">
    <div class="col-md-12">
      <div class="ibox float-e-margins">
        <div class="ibox-content">
          <% title "All Subjects" %>
          <% flash.each do |key, value| %>
              <div class="alert alert-<%= key %>">
                <% if value.kind_of?(Array) %>
                  <ul>
                    <% value.each do |msg| %>
                      <li><%= msg %></li>
                    <% end %>
                  </ul>
                <% else %>
                  <%= value %>
                <% end %>
              </div>
          <% end %>
          <% if @subjects.blank? %>
            <p>There are not any subjects currently in the system.</p>
            <p>
              <%= link_to new_subject_path, class: "btn btn-success" do %>
                <%= fa_icon "plus", text: "Add Subject"%>
              <% end %>
            </p>
          <% else %>
            <p>These are the current subjects in our system.</p>
            <p>
              <%= link_to new_subject_path, class: "btn btn-success" do %>
                <%= fa_icon "plus", text: "Add Subject"%>
              <% end %>
            </p>
            <div class="table-responsive">
              <table class="table table-striped">
                <thead>
                  <tr>
                    <td>No</td>
                    <td>Subject Name</td>
                    <td>Description</td>
                    <td>Status</td>
                    <td>Action</td>
                  </tr>
                </thead>
                <tbody>
                  <% i = 1 %>
                  <% @subjects.each do |c| %>
                    <tr>
                      <td><%= i %></td>
                      <td><%= c.nama %></td>
                      <td><%= c.description %></td>
                      <td>
                        <% if c.status == '1' %>
                          <%= fa_icon 'check', style: 'color:green' %>
                        <% else %>
                          <%= fa_icon 'close', style: 'color:red' %>
                        <% end %>
                      </td>
                      <td>
                          <%= link_to edit_subject_path(c), class: 'btn btn-warning', title: 'Edit' do %>
                            <%= fa_icon 'edit' %>
                          <% end %>
                          <%= link_to subject_path(c), class: 'btn btn-info', title: 'Detail' do %>
                            <%= fa_icon 'list' %>
                          <% end %>
                          <%= link_to subject_path(c), class: 'btn btn-danger', title: 'Delete', method: :delete, data: { confirm: 'Are you sure?'} do %>
                            <%= fa_icon 'trash' %>
                          <% end %>
                      </td>
                    </tr>
                  <% i += 1 %>
                  <% end %>
                </tbody>
              </table>
            </div>

          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
